[build]
builder = "NIXPACKS"

[build.nixpacksPlan.phases.setup]
nixPkgs = ["python311", "python311Packages.pip"]

[deploy]
startCommand = "gunicorn backend_server:app --bind 0.0.0.0:8080"
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 10